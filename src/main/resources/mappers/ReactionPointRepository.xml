<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.example.demo.repository.ReactionPointRepository">

	<select id="getReactionPointCntByArticleId" parameterType="int"
		resultType="int">
		SELECT COUNT(*)
		FROM reactionPoint
		WHERE relTypeCode =
		'article' AND relId =${articleId} AND `point` = ${type}
	</select>

	<select id="getReactionPointCntByMemberId" parameterType="int"
		resultType="int">
		SELECT COUNT(*)
		FROM reactionPoint
		WHERE relTypeCode =
		'article' AND relId =#{id} AND memberId = #{memberId}
	</select>

	<select id="insertReactionPoint" parameterType="map">
		INSERT INTO
		reactionPoint
		SET regDate = NOW(),
		updateDate = NOW(),
		memberId =
		#{memberId},
		relTypeCode = 'article',
		relId = #{articleId},
		point = #{type}
	</select>
	
	<select id="deleteReactionPoint" parameterType="map">
		DELETE FROM
		reactionPoint
		WHERE memberId =#{memberId}
		AND relTypeCode = 'article'
		AND relId = #{articleId}
	</select>
	
	<select id="updateReactionPoint" parameterType="map">
		UPDATE	reactionPoint
		SET point = #{type}
		WHERE memberId =#{memberId}
		AND relTypeCode = 'article'
		AND relId = #{articleId}
		
	</select>
	
	

</mapper>